# RAG Documents

Place `.md`, `.markdown`, or `.txt` files in one of these folders:

- `center_policy`
- `parent_handbook`
- `faq`
- `course_description`
- `announcement`

The backend reads these folders and ingests each file into RAG document chunks.
